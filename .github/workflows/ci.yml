name: CI

# Gate de calidad: cada push/PR valida sintaxis + pruebas críticas + seguridad de reglas.
# Objetivo: que ningún bug llegue a producción (los runners de GitHub SÍ tienen Java, así que
# aquí SÍ corren las pruebas del emulador de Firestore que no pueden correr en cualquier máquina).

on:
  push:
    branches: [ main, 'respaldo/**' ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Node 20
        uses: actions/setup-node@v6
        with:
          node-version: '20'

      - name: Java 17 (para el emulador de Firestore)
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Instalar dependencias (raíz)
        run: npm install

      - name: Instalar dependencias (functions, para el emulador)
        run: npm --prefix functions install

      - name: Verificar sintaxis (módulo de index.html + sw.js)
        run: npm run check

      - name: Pruebas críticas (node:test — flujos críticos + módulos js/core)
        run: npm test

      - name: Pruebas de SEGURIDAD de reglas + Cloud Function (emulador)
        run: npm run test:emu
